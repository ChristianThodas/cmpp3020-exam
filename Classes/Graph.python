class Graph:
    def __init__(self):
        self.alter = []
    def add_route(self, route):
        if route.fromstop not in self.alter:
            self.alter[route.fromstop].append(route)
 
    def get_routes(self, stop):
        return self.alter.get(stop, [])
        
       